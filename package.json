{
  "name": "poker-game",
  "version": "1.0.0",
  "scripts": {
    "build": "npm install --prefix server",
    "start": "node server/index.js",
    "dev": "npm start --prefix client"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
